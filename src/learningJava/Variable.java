package learningJava;

import java.util.Scanner;

public class Variable {

	public static void main(String[] args) {
/*Variable is a memory in the system that holds a value. A value of a variable can change but the type of a variable always stays same. 
 * An int variable can never hold a double or string. 
 * It is permitted to declare several variables in a single declaration and simultaniously to assign them initial values. 
 * For instance, the following code segment initializes the variable z, q, pi, name and reader.
 */
		
		int x,y,z = 7;
		double p,q = 1.444;
		String name="My name is nowshin";
		Scanner reader = new Scanner(System.in);
		int a,b,c; b=4; //Declare three integer variables (a,b,c) in a single declaration and simultaniously initialize b to be 4. 
		
		
	}

}
